# 2026-04-16 生产同步记录

## 背景

本次目标是把本地已经完成的 UI 更新完整同步到生产环境，同时把支撑这些页面的权限、审批、审计与数据范围能力一并发布，避免出现“页面上线了，但接口和表结构没有跟上”的不一致状态。

## 发布目标

- 管理中心四页正式上线：
  - 成员管理
  - 角色权限
  - 审批规则
  - 操作日志
- 首页切换为按角色展示的工作台
- 顶部通知中心改为铃铛抽屉
- 左侧导航调整为：
  - 首页
  - 日程
  - 客户
  - 产品
  - 方案
  - 报价
  - 档案
  - 管理中心
  - 设置
- 报价详情补齐审批状态、审批处理和导出权限控制

## 同步方式

- 源代码来源：本地工作区当前状态
- 目标服务器：`root@49.232.57.98`
- 目标目录：`/opt/huigui-crm`
- 容器方式：`docker compose`
- 执行脚本：`scripts/ops/deploy-local-to-production.sh`

## 本次同时上线的支撑能力

- RBAC 角色权限体系
- 数据范围权限
- 折扣审批、低价审批、正式报价导出审批、客户转移审批
- 审计日志结构增强
- 成员账号字段扩展
- 管理中心所需 Prisma migration 和 seed

## 发布后核对清单

- Docker 容器 `huigui-app`、`huigui-api`、`huigui-nginx` 正常运行
- API 健康检查返回 `{"status":"ok"}`
- 登录页可访问
- 管理员登录后，管理中心总览 API 返回正常
- 新 migration 已进入生产数据库
- seed 已更新系统角色、权限和审批规则
- 服务器备份文件：`/opt/huigui-backups/huigui-crm-ui-sync-20260416-103627.tar.gz`

## 后续约定

- 以后所有生产同步优先走 `scripts/ops/deploy-local-to-production.sh`
- 每次发版都要在 `docs/deployment-log.md` 追加一条记录
- 如果本次发布包含新增页面或权限逻辑，必须同时记录：
  - 页面范围
  - 是否涉及数据库 migration
  - 是否需要重新 seed
  - 发布后验证结果
- 如果只是改了服务器源码但没有留下文档记录，视为未完成发布
